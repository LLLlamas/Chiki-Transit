# Copy this to .env.local for local development.
# Never commit .env.local — it is gitignored.

# URL of the deployed Cloudflare Worker proxy
VITE_PROXY_URL=https://chiki-transit-proxy.lorenzoleollamas.workers.dev
